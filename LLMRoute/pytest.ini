[pytest]
testpaths = tests
markers =
    real_ollama_probe: exercises the actual Ollama TCP reachability probe instead of the autouse mock
filterwarnings =
    ignore::DeprecationWarning
    ignore:.*create_react_agent has been moved.*:UserWarning
